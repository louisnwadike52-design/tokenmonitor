---
name: Bug report
about: Something is broken or a log isn't parsed correctly
labels: bug
---

**What happened?**

**What did you expect?**

**Environment**
- tokenmonitor version (`tokenmonitor --version`):
- Node version (`node --version`):
- OS:
- AI CLI + version whose logs are affected (if applicable):

**Sanitized log sample** (for parsing bugs — a few offending lines with any
private content removed; the shape matters, not the content):

```json
```
